package leetcode;

public class SearchinRotatedSortedArray {
	public int search(int[] nums, int target) {
        int left = 0;
        int right = nums.length - 1;
        int mid = (left + right) / 2;
        mergeFind(left, right, mid, nums);
    }

	private void mergeFind(int left, int right, int mid, int[] nums) {
		if (right - left < 2) {
			
		}
	}
}
